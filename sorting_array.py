import numpy as np 
arr = np.array([3,2,0,1])
print(np.sort(arr)) # this is used to sort the array in ascending order

#alphabetical order
import numpy as np 
arr = np.array(['banana', 'cherry', 'apple'])
print(np.sort(arr))# this is used to sort the array in alphabetical order

#2D array
import numpy as np
arr = np.array([[3,2,4],[5,0,1]])
print(np.sort(arr)) # this is used to sort the 2D array in ascending order